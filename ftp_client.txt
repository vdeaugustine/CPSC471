Import the argparse, socket, sys, cmd, and os.

Set up the arguments by assigning the server name and port number into the argument.

If server port number is not digit:
	Print "Port number not valid" and exit.
Else:
	Set server port number as int(server port number).
	
try:
	Try to create a new client socket (IPv4, TCP format) if possible.

	Now try to connect to the server with its name and port number if possible.

	Prompt the user to enter the FTP command.
	Continue to loop through the command indefinitely until user enters "quit".
except socket error:
	Print "An unexpected error has occured."

Close the client socket.
